# =================================
# HOT RELOAD (macOS only)
# =================================

# Set to true on macOS to enable filesystem polling for Vite hot reload inside Docker.
# Not needed on Linux.
CHOKIDAR_USEPOLLING=false
