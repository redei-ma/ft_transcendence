import { Player, IPlayerState } from "@transcendence/types";
export declare class DefenceAttack implements IPlayerState {
    private timer;
    private duration;
    onEnter(player: Player): void;
    update(player: Player, dt: number): boolean;
}
//# sourceMappingURL=player.defenceAttackState.d.ts.map